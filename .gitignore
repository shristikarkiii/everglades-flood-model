# Downloaded and derived rasters. Everything here is reproducible with
# `python run.py`; the DEM, land cover, surface water and Sentinel-1 archives
# are large and openly redistributed by their providers.
data/

# The continuous index raster is 27 MB and is regenerated by the pipeline.
# The classified raster (1.8 MB) is the actual deliverable and is committed,
# as are the figures and results.json, so the repository is useful without
# running anything.
outputs/flood_susceptibility_index.tif

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
verify_run.log
verify_run.err
.env
